---
title: "Welcome to the Journal"
date: "2026-07-27"
videoUrl: ""
excerpt: "A quiet space, off the main path."
---

This is your first journal entry. Replace this text, add a video link in the frontmatter above if you have one, and this page becomes a real post.

To add a new entry:

1. Duplicate this file inside `content/vlog/` and rename it — the filename becomes the URL slug (e.g. `training-update.md` → `/vlog/training-update`).
2. Fill in `title`, `date`, and optionally `videoUrl` (a YouTube or Vimeo URL) at the top.
3. Write the body in Markdown below the `---`.
4. Commit and push — the site rebuilds and the new page goes live at its own private URL.

Nothing here is linked from the main site or indexed by search engines. The only way in is a direct link.
